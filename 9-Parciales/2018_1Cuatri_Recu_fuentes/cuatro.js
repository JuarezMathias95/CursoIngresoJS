function mostrar()
{
    var CantidadDeLibros = prompt("Cuantos libros desea llevar");
    CantidadDeLibros = parseInt(CantidadDeLibros);
    var PrimerImporte = prompt("Cual es el total sin ningun descuento?");
    PrimerImporte = parseInt(PrimerImporte);
    var PrimerRecargo = (PrimerImporte * 10) / 100;
    PrimerRecargo = parseInt(PrimerRecargo);
    var SegundaOferta = (PrimerRecargo * 15 ) / 100;
    SegundaOferta = parseInt(SegundaOferta);
    var ValorTotal
    ValorTotal = parseInt(ValorTotal);
    var tarjeta
    var efectivo 
      
    if (CantidadDeLibros >= 2 )  {
    CantidadDeLibros = PrimerRecargo }

    {
        if (CantidadDeLibros >= 2 && PrimerImporte >= 2000)
    {    ValorTotal = SegundaOferta }

}

}
